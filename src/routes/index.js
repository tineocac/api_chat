const userRouter = require("./users.routes");
const authRouter = require("./auth.routes");
const converationsRouter = require("./conversations.routes");

module.exports = {
  userRouter,
  authRouter,
  converationsRouter,
};
